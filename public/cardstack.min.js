(() => { const random = (min, max) => Math.floor(Math.random() * (max - min + 1) + min), pallettes = [["#F38181", "#FCE38A", "#EAFFD0", "#95E1D3", "#5F7367"], ["#A2D2FF", "#FEF9EF", "#FF865E", "#FEE440", "#00798C"]], randomColorFromPallette = pallette => { if (!pallette.length) return console.error("Color pallette array empty or not big enough. Setting all cards to color black."), "#000"; const randomIndex = Math.floor(pallette.length * Math.random()); return pallette.splice(randomIndex, 1)[0] }; let tempColors = Array.from(pallettes[random(0, pallettes.length - 1)]); const allCards = $$(".cardstack"); for (let i = 0; i < allCards.length; i++) { const card = allCards[i]; card.style.zIndex = i + 1; const randomColor = randomColorFromPallette(tempColors); card.style.backgroundColor = randomColor; const randomTransitionTime = .1 * random(1, 10); card.style.setProperty("--transition-time", randomTransitionTime + "s"), card.style.setProperty("--random-x", `${random(-40, -55)}%`), card.style.setProperty("--random-y", `${random(-40, -55)}%`), card.style.setProperty("--random-rotation", `${random(-30, 30)}deg`) } })();